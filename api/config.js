// 定义数据库连接参数
const config = {
  url: "localhost",
  port: 3000,
  db: {
    host: "localhost",
    user: "root",
    pwd: "123456",
    dataBase: "wall",
  },
};

// 导出config
module.exports = config;
